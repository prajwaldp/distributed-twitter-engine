defmodule TwitterEngineWeb.PageControllerTest do
  use TwitterEngineWeb.ConnCase

  test "GET /", %{conn: conn} do
    conn = get(conn, "/")
    assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  end
end
